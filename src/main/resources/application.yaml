file:
  client-path: /file
  storage-location: C:\Users\ASUS PC\Pictures\ite-3re\
  base-uri: http://localhost:${server.port}${file.client-path}/

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: table
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${URL}
    username: ${USERNAME}
    password: ${PASSWORD}
  application:
    name: ite-3rd-ecommerce

server:
  port: 8081

