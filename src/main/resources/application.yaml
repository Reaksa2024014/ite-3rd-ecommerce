file:
  client-path: /file
  storage-location: C:\Users\ASUS PC\Pictures\ite-3re\
  base-uri: http://localhost:${server.port}${file.client-path}/
spring:
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9999/realms/ite-ecommerce
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: table
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${URL}
    username: ${USERNAME}
    password: ${PASSWORD}
  application:
    name: ite-3rd-ecommerce
server:
  port: 8081
keycloak:
  server-url: http://localhost:9999
  realm: master
  target-realm: ite-ecommerce
  client-id: ite-ecommerce-spring-api
  client-secret: CY7PxXmWZqluv60OoAuwcPVmZORYRM2g

